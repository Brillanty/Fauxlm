<%= form_with(model: article) do |form| %>
  <% if article.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(article.errors.count, "error") %> prohibited this article from being saved:</h2>

      <ul>
        <% article.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class= "mb-3">
    <%= form.label :title, class: "form-label", style: "font-size:24px"%>
    <%= form.text_field :title, class: "form-control", placeholder: "Title" %>
  </div>

  <div class= "mb-3">
    <%= form.label :chapo, class: "form-label", style: "font-size:24px" %>
    <%= form.text_area :chapo, class: "form-control", placeholder: "Title" %>
  </div>

  <div class= "m-4">
    <%= form.label :body, class: "form-label", style: "font-size:24px" %>
    <%= form.text_area :body, class: "form-control", placeholder: "Body" %>
  </div>

  <div class="m-4">
    <%= form.submit %>
  </div>
<% end %>
